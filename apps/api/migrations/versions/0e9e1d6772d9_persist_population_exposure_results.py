"""persist population exposure results"""

from alembic import op
import sqlalchemy as sa
from sqlalchemy.dialects import postgresql

revision = "0e9e1d6772d9"
down_revision = "89a3ce782491"
branch_labels = None
depends_on = None


def upgrade():
    # ### commands auto generated by Alembic - please adjust! ###
    op.create_table(
        "population_exposure_results",
        sa.Column("analysis_run_id", sa.Uuid(), nullable=False),
        sa.Column("population_source_version_id", sa.Uuid(), nullable=False),
        sa.Column("population_year", sa.Integer(), nullable=False),
        sa.Column("estimated_total", sa.Float(), nullable=False),
        sa.Column("estimated_valid_coverage", sa.Float(), nullable=False),
        sa.Column("metrics", postgresql.JSONB(astext_type=sa.Text()), nullable=False),
        sa.Column("object_key", sa.String(), nullable=False),
        sa.Column("checksum_sha256", sa.String(length=64), nullable=False),
        sa.Column("id", sa.Uuid(), nullable=False),
        sa.Column("created_at", sa.DateTime(timezone=True), nullable=False),
        sa.CheckConstraint(
            "estimated_total >= 0 AND estimated_valid_coverage >= 0 AND estimated_valid_coverage <= estimated_total + 0.1",
            name="population_count_balance",
        ),
        sa.ForeignKeyConstraint(
            ["analysis_run_id"],
            ["analysis_runs.id"],
        ),
        sa.ForeignKeyConstraint(
            ["population_source_version_id"],
            ["source_versions.id"],
        ),
        sa.PrimaryKeyConstraint("id"),
        sa.UniqueConstraint("analysis_run_id"),
        sa.UniqueConstraint("object_key"),
    )
    # ### end Alembic commands ###


def downgrade():
    if op.get_bind().scalar(sa.text("SELECT EXISTS (SELECT 1 FROM population_exposure_results)")):
        raise RuntimeError("Population results exist; preserve them before downgrading")
    # ### commands auto generated by Alembic - please adjust! ###
    op.drop_table("population_exposure_results")
    # ### end Alembic commands ###
